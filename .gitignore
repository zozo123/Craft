.DS_Store
Makefile
CMakeCache.txt
CMakeFiles
cmake_install.cmake
config.py
craft
world
env
log.txt
*.o
*.db
*.exe
*.dll
*.pyc

# The bare `Makefile` rule above targets CMake's generated root makefile;
# the oracle harness ships its own and must stay tracked.
!oracle/Makefile
oracle/build/

# Rust build output.
rust/target/

# Golden fixtures are generated by the C oracle on each machine (float results
# are platform-specific), so they are never committed.
fixtures/golden/

